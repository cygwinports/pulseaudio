--- origsrc/pulseaudio-5.0/src/pulse/util.c	2014-01-21 07:39:57.000000000 -0600
+++ src/pulseaudio-5.0/src/pulse/util.c	2014-11-30 00:03:54.341515700 -0600
@@ -181,7 +181,7 @@ char *pa_get_binary_name(char *s, size_t
     }
 #endif
 
-#ifdef __linux__
+#if defined(__linux__) || defined(__CYGWIN__)
     {
         char *rp;
         /* This works on Linux only */
--- origsrc/pulseaudio-5.0/src/pulsecore/core-util.c	2014-03-03 07:52:41.000000000 -0600
+++ src/pulseaudio-5.0/src/pulsecore/core-util.c	2015-01-15 17:02:44.509250200 -0600
@@ -2589,7 +2589,7 @@ int pa_close_allv(const int except_fds[]
     struct rlimit rl;
     int maxfd, fd;
 
-#ifdef __linux__
+#if defined(__linux__) || defined(__CYGWIN__)
     int saved_errno;
     DIR *d;
 
--- origsrc/pulseaudio-5.0/src/pulsecore/pid.c	2014-01-23 12:57:55.000000000 -0600
+++ src/pulseaudio-5.0/src/pulsecore/pid.c	2014-11-30 00:07:17.909159100 -0600
@@ -136,7 +136,7 @@ fail:
 }
 
 static int proc_name_ours(pid_t pid, const char *procname) {
-#ifdef __linux__
+#if defined(__linux__) || defined(__CYGWIN__)
     char bn[PATH_MAX];
     FILE *f;
 
@@ -337,7 +337,7 @@ int pa_pid_file_kill(int sig, pid_t *pid
     char *fn;
     int ret = -1;
     pid_t _pid;
-#ifdef __linux__
+#if defined(__linux__) || defined(__CYGWIN__)
     char *e = NULL;
 #endif
 
@@ -381,7 +381,7 @@ fail:
         errno = saved_errno;
     }
 
-#ifdef __linux__
+#if defined(__linux__) || defined(__CYGWIN__)
     pa_xfree(e);
 #endif
 
--- origsrc/pulseaudio-5.0/src/pulsecore/shm.c	2014-01-23 12:57:55.000000000 -0600
+++ src/pulseaudio-5.0/src/pulsecore/shm.c	2014-11-30 00:08:19.455679300 -0600
@@ -63,7 +63,7 @@
 /* 1 GiB at max */
 #define MAX_SHM_SIZE (PA_ALIGN(1024*1024*1024))
 
-#ifdef __linux__
+#if defined(__linux__) || defined(__CYGWIN__)
 /* On Linux we know that the shared memory blocks are files in
  * /dev/shm. We can use that information to list all blocks and
  * cleanup unused ones */
